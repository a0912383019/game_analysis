server {
  listen         $PORT;
  server_name    localhost;
  error_page     404 index.html;

  location / {
    root /usr/share/nginx/html;
    #index index.html
    try_files $uri /index.html;
  }
}